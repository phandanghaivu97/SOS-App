<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="capstone1.dtu.f4.sosapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Receive_SOS" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/duongvien_goctron" />

        <activity android:name=".Off_Report" />
        <activity android:name=".Register" />
        <activity android:name=".Send_SOS" />
        <activity android:name=".Login_Activity" />
        <activity android:name=".activity_login_police">

        </activity>
        <activity android:name=".receive_sos_police" >

        </activity>
        <activity android:name=".main_sos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>